# 项目协作约定

## 功能创意的持久记录

`FEATURE_IDEAS.md` 是本项目功能建议的唯一登记册。每次涉及产品功能的讨论、建议、采纳、拒绝或实现，均须先完整阅读它，并在本次变更中同步更新，不能等待用户提醒。

- 新建议先按用户问题、核心动作和价值与已有条目做语义去重；仅是改名、换界面或拆分的内容不新增。
- 尊重其中的永久排除方向；用户明确撤销排除前不得提出或实施。
- 新建议登记到 Backlog，使用下一个连续 `B` 编号，并包含优先级、价值和与现有能力的边界。
- 被拒绝的方向登记到黑名单；开始实施的条目标记进行中；完成后移入“已实现”，并保留编号与落地文件。
- 完成更新后确认编号唯一、每个创意只存在于一个状态区，并在最终答复中说明登记结果。
- 用户请求“推荐”“继续建议”或“还有什么”时，默认一次提供至少 10 个经过语义去重的新方向；只有用户明确限定数量或主题时才缩减范围。
